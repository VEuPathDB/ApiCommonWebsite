PLACEHOLDER — user-message turns for the getGeneSummary stage.

One paragraph per user-message turn; a blank line separates turns (the Anthropic
Java SDK adds one addUserMessage per turn). Placeholder markers substituted at
render time via PromptLoader (simple String.replace, matching the Python
convention):

[PAPER_TEXT]
[GENE]
[N_QUOTES]
[JSON_SCHEMA]

Real content ported in deliverable 4. The assistant turn is prefilled with "{".

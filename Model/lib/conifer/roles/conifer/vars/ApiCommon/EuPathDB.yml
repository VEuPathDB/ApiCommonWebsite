_host_frontends:
  a1: alpha
  a2: alpha
  b1: beta
  b2: beta
  q1: qa
  q2: qa
  w1: ''
  w2: ''

projectsxml:
  AmoebaDB: "{{ hostname|swap_sld('amoebadb.org')|swap_hostname(_host_frontends) }}/amoeba/"
  CryptoDB: "{{ hostname|swap_sld('cryptodb.org')|swap_hostname(_host_frontends) }}/cryptodb/"
  FungiDB: "{{ hostname|swap_sld('fungidb.org')|swap_hostname(_host_frontends) }}/fungidb/"
  GiardiaDB: "{{ hostname|swap_sld('giaridadb.org')|swap_hostname(_host_frontends) }}/giardiadb/"
  MicrosporidiaDB: "{{ hostname|swap_sld('microsporidiadb.org')|swap_hostname(_host_frontends) }}/micro/"
  PiroplasmaDB: "{{ hostname|swap_sld('piroplasmadb.org')|swap_hostname(_host_frontends) }}/piro/"
  PlasmoDB: "{{ hostname|swap_sld('plasmodb.org')|swap_hostname(_host_frontends) }}/plasmo/"
  ToxoDB: "{{ hostname|swap_sld('toxodb.org')|swap_hostname(_host_frontends) }}/toxo/"
  TrichDB: "{{ hostname|swap_sld('trichdb.org')|swap_hostname(_host_frontends) }}/trichdb/"
  TriTrypDB: "{{ hostname|swap_sld('tritrypdb.org')|swap_hostname(_host_frontends) }}/tritrypdb/"

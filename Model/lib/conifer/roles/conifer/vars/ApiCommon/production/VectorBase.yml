# $SourceFileURL$
# --
eda:
  enabled_env_map:
    w_prefix: "false"
    q_prefix: "false"
    b_prefix: "true"
    default: "true"
  enabled: "{{ enabled_env_map[prefix]|default(enabled_env_map['default']) }}"


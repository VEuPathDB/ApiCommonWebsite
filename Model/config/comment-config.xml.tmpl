<?xml version="1.0"?>

<commentConfig 
    login="${commentDb.login}"
    password="${commentDb.password}"
    connectionUrl="jdbc:oracle:oci:@${commentDb.instance}"
    platform="${commentDb.platform}"
    maxActive="${commentDb.maxActive}"
    maxIdle="${commentDb.maxIdle}"
    minIdle="${commentDb.minIdle}"
    maxWait="${commentDb.maxWait}"
    commentSchema="${commentDb.commentSchema}"
    userFileSchema="${commentDb.userFileSchema}"
    userFileUploadDir="${commentDb.userFileUploadDir}"
/>

